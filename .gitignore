# Data (downloaded by scripts/download_data.sh — never committed)
data/
!data/.gitkeep

# Quarto / Jupyter scratch
.quarto/
_freeze/
cache/

# Root-level deliverable render (the servable copy lives in docs/)
/deliverable.pdf
deliverable.typ
.ipynb_checkpoints/
*_files/
__pycache__/

# Large analysis outputs are regenerated; keep small CSV summaries only
results/*.h5ad

# OS
.DS_Store
Thumbs.db

/.quarto/
**/*.quarto_ipynb
